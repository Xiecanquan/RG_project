# 背单词软件技术设计文档

**版本:** v1.5
**日期:** 2025年11月13日
**作者:** AI 助手
**修订记录:**
| 版本 | 日期 | 描述 |
| :-- | :-- | :-- |
| 1.0 | 2025-10-25 | 初始技术方案设计 |
| 1.1 | 2025-10-25 | 移除视频生成模块，明确AI例句生成服务商 |
| 1.2 | 2025-10-25 | 明确词典和发音方案，细化AI例句生成与词书导入逻辑，更新待定风险 |
| 1.3 | 2025-10-26 | **重大架构调整：从桌面应用转向前后端分离的Web应用，数据库迁移至MySQL** |
| 1.4 | 2025-10-27 | **关键改进：统一时区处理（北京时间）、JSON解析安全性、Prisma单例优化** |
| 1.5 | 2025-11-13 | **V2架构升级：简化为二层结构（Word → Meaning），partOfSpeech改为字符串，新增lemma支持** |

---

### 1. 引言

#### 1.1 目标

本文档旨在为背单词软件提供一份全面的技术设计方案，涵盖软件架构、技术栈选择、核心模块设计、数据流和部署策略。目标是构建一个高效、稳定、易于维护和扩展的 **Web 网站应用**。

#### 1.2 范围

本技术文档主要针对产品的MVP（最小可行产品）阶段，以 **Web 网站应用** 为主要形态。视频生成功能暂时搁置。由于架构变更为 Web 应用，数据将集中存储在服务器，天然支持多端数据同步。

#### 1.3 背景

根据产品需求文档（PRD），本软件旨在通过创新的学习和复习机制、个性化词书管理以及AI赋能，帮助学生高效掌握单词。

### 2. 总体架构

我们将采用经典的 **前后端分离** 架构。这种模式将应用分为三个核心部分：前端、后端和数据库，实现了逻辑与视图的解耦，非常适合现代 Web 应用的开发与维护。

![架构图](https://mermaid.live/svg/pako:eNqNkstuwjAQhl_F5S4eN4GKBBEpUFWpVSq1D9A4bsBOYh_YhYqiK_Eu3SbdpM-kKzGgQJbM_P_PzPjF2JgY2iYIuHPU6sd8x6FvKZrWl10v0SvN_mHevUavwO5CmlbI_xXIO0rDsubG-c61vYpXfN33vM-jGv1e8fFf-S11J3-O-u1dK_sH7n-dE_yQ_fG1dJ4lI-qFkO_G-1P4W79j4uT1_Xq-jT0x_wK-8R5G)

**架构说明：**

*   **前端 (Frontend):** 运行在用户浏览器中，负责用户交互和视觉呈现。使用 React、TypeScript 和 Tailwind CSS 构建。
*   **后端 (Backend):** 运行在服务器上，是应用的大脑。负责处理所有业务逻辑、用户认证、数据库交互、调用AI服务等。
*   **数据库 (Database):** 采用 MySQL 数据库，集中存储所有数据，包括用户信息、单词库、学习进度、词书等。
*   **AI 服务 (AI Services):** 作为独立的外部服务，由后端进行调用，以实现例句生成等高级功能。

### 3. 技术栈选择及理由

#### 3.1 后端框架：Node.js + Express.js

*   **选择：Node.js + Express.js**
*   **理由：**
    *   **统一语言：** 后端使用与前端相同的 TypeScript/JavaScript 语言，降低学习成本和开发复杂性。
    *   **成熟生态：** Node.js 拥有庞大的 npm 包生态系统，可以轻松找到各种功能的库（如数据库驱动、API验证工具等）。
    *   **易于上手：** Express.js 是一个轻量且灵活的框架，API 简洁，非常适合快速构建 MVP 版本的后端服务。
    *   **性能适中：** 对于 API 服务这类 I/O 密集型应用，Node.js 的异步非阻塞模型表现良好。

#### 3.2 前端技术：React + TypeScript + Tailwind CSS

*   **选择：React + TypeScript + Tailwind CSS**
*   **理由：**
    *   **React：** 强大的组件化开发模式，能够构建复杂的交互界面。您提到的 `bolt.new` 工具能快速生成此技术栈的项目，加速开发。
    *   **TypeScript：** 为 JavaScript 提供静态类型检查，提高代码健壮性和可维护性，尤其适合构建需要长期维护的项目，对于编程新手养成良好习惯也非常有益。
    *   **Tailwind CSS：** 原子化 CSS 框架，能快速构建美观、现代化的界面，符合您对 UI/UX 的偏好。

#### 3.3 数据库：MySQL

*   **选择：MySQL**
*   **理由：**
    *   **功能强大稳定：** MySQL 是全球最受欢迎的开源关系型数据库之一，成熟、稳定、可靠，完全能满足本应用对结构化数据的存储需求。
    *   **生态丰富：** 拥有丰富的客户端工具、文档和社区支持，遇到问题容易解决。
    *   **标准 SQL：** 使用标准 SQL，便于数据查询、迁移和管理。
*   **ORM (强烈推荐):** 推荐使用 **Prisma** 或 **TypeORM**。ORM 工具能让您用 TypeScript/JavaScript 对象来操作数据库，替代手写 SQL，极大地提高开发效率、代码可读性和类型安全性。

#### 3.4 核心编程语言：TypeScript

*   **选择：TypeScript**
*   **理由：**
    *   **全栈统一：** 前端（React）和后端（Node.js）都使用 TypeScript 开发，实现技术栈统一，便于代码复用和人员协作。
    *   **类型安全：** TypeScript 的类型系统能在编译阶段捕获大量潜在错误，提升代码质量和项目稳定性。

#### 3.5 AI 服务与词典集成

*   **例句生成 (AI服务):**
    *   **实现位置：** **后端服务器**。
    *   **推荐：** 优先探索 DeepSeek、百度千帆、阿里云通义千问等 API。
    *   **理由：** 由后端统一调用和管理 AI 服务。这些国产大模型在处理中文语境下的文本生成任务方面具有天然优势，并且其API服务通常会提供免费额度或具有竞争力的价格。将 API Key 等敏感信息安全地存储在服务器环境变量中，避免泄露到前端。同时，可以采用统一的API调用层，方便未来切换或集成多个AI服务。
*   **基础词典数据 (开源):**
    *   **实现位置：** **后端服务器**。
    *   **理由：** 在服务器端整合开源词典数据（如解析特定格式的词典文件），构建成服务内部的数据源，供后端逻辑调用。这能确保基础词典功能的稳定性和免费性，避免初期对外部 API 的过度依赖。
*   **单词发音：**
    *   **实现位置：** **前端浏览器**。
    *   **推荐：** 浏览器内置语音合成 (Web Speech API)。
    *   **理由：** 免费、易于实现，直接利用客户端能力，不消耗服务器资源。

#### 3.6 部署

*   **MVP 阶段 (本地部署):**
    *   **前端：** 通过 Vite 或 Create React App 的开发服务器运行。
    *   **后端：** 通过 `ts-node` 或 `nodemon` 在本地运行 Node.js 服务器。
    *   **数据库：** 在本地安装并运行 MySQL 服务器。
    *   **说明：** 这种方式完全满足您“先实现在我的电脑上的部署”的需求，便于快速开发和调试。
*   **生产阶段 (云部署):**
    *   **前端：** 可以部署到 Vercel, Netlify 等静态托管平台。
    *   **后端与数据库：** 可以使用云服务器（如阿里云、腾讯云）配合 Docker 进行容器化部署，或使用 Serverless 平台和云数据库服务。

### 4. 模块设计 (技术实现思路)

*   **4.1 核心学习模块：**
    *   **前端：** React组件负责展示单词、例句、多选词义等UI。通过状态管理（如Zustand）管理学习状态，并通过API请求从后端获取学习数据。
    *   **后端：** 提供API接口，根据用户ID和学习进度，从MySQL数据库中查询下一个要学习的单词和相关词义，并返回给前端。用户完成学习后，接收前端请求，将学习进度更新到 `user_learning_progress` 表中（关联到具体的 `meaning_id`）。
*   **4.2 复习模块：**
    *   **前端：** 展示不同复习模式（拼写、词义选择）的界面。
        *   **拼写模式（production）：** 显示词义提示框（词性+释义）、例句（单词位置用下划线）、输入框，用户拼写单词后提交。
        *   **选择题模式（recognition）：** 显示单词、例句（高亮单词）、4个选项，用户选择正确含义。
    *   **后端：** 提供API接口，根据艾宾浩斯算法和用户ID，从MySQL中查询当天需要复习的 **词义ID (`meaning_id`)** 列表。对于每个需要复习的词义，通过 `meaning_id` -> `word_id`，并结合用户的 `current_book_tag_id`，在 `word_tag_relations` 表中查出对应的 `mastery_focus`，从而决定复习方式，并将完整的复习数据（包含词义、例句等）返回给前端。
*   **4.3 词书管理模块：**
    *   **前端：** 提供词书导入、选择、删除等操作界面。
    *   **后端：**
        *   **纯文本导入：** 提供API接口接收用户上传的单词列表。解析文本，查询MySQL数据库。对新单词，**优先调用服务器整合的开源词典数据获取基础释义，不足时**再调用AI服务补全信息，然后存入数据库。
        *   **词书标签管理：** 提供API接口对 `book_tags` 和 `word_tag_relations` 表进行增删改查。
*   **4.4 AI 赋能模块：**
    *   **前端：** 触发需要AI参与的功能时，向后端发送API请求，并展示加载状态。
    *   **后端：** 接收前端请求，构建Prompt，调用选定的LLM API，处理返回结果后，再响应给前端。**视频生成功能暂时搁置**。
*   **4.5 用户认证模块：**
    *   **前端：** 提供注册和登录表单。登录成功后，保存后端返回的Token（如JWT），并在后续所有API请求的请求头中携带该Token。
    *   **后端：** 提供注册和登录API。验证用户信息，生成Token并返回给前端。为需要登录才能访问的API设置中间件，验证Token的有效性。
*   **4.6 其他基础功能：**
    *   **单词发音/例句朗读：** 前端调用浏览器内置 Web Speech API 实现。
    *   **生词本/学习进度统计：** 后端提供API接口，从MySQL查询相关数据。前端通过图表库（如Chart.js, Recharts）进行可视化展示。
    *   **每日打卡：** 后端提供API接口处理打卡逻辑，记录数据到数据库，包括连续打卡天数计算、成就解锁（记录在 `user_achievements` 表）和鼓励语展示。

### 5. 数据流设计

1.  **用户操作：** 用户在浏览器（前端）进行学习、复习、登录等操作。
2.  **前端API请求：** 前端React应用通过 `fetch` 或 `axios` 向后端服务器发送HTTP API请求（如 GET /api/words/next, POST /api/login）。
3.  **后端逻辑处理：** 后端Express服务器接收到请求，执行相应逻辑：
    *   **用户认证：** 通过中间件验证请求中的Token。
    *   **数据库操作：** 使用ORM（如Prisma）对MySQL数据库进行增删改查。
    *   **AI服务调用：** 异步调用外部AI API获取例句。
    *   **词典数据查询：** 查询服务器整合的开源词典数据。
4.  **后端API响应：** 后端将处理结果（数据或状态码）以JSON格式返回给前端。
5.  **UI更新：** 前端接收到响应数据，更新React组件状态，重新渲染界面。

### 6. 安全与性能考虑

#### 6.1 安全性

*   **用户认证与授权：** 使用JWT (JSON Web Tokens) 或 Session-Cookie机制进行用户身份验证，确保API只能被授权用户访问。
*   **密码安全：** 用户密码在存入数据库前，必须使用 `bcrypt` 等强哈希算法进行加盐哈希。
*   **数据解析安全：** 所有JSON解析操作使用安全包装函数（`safeJsonParse`），避免格式错误导致服务崩溃。
*   **时区统一处理：** 所有时间操作统一使用北京时间（UTC+8），确保打卡、复习时间计算的准确性。

#### 6.2 性能

*   **数据库优化：** 对MySQL中的常用查询字段建立索引，优化复杂查询，确保数据操作效率。**批量查询优化：避免在循环中执行数据库查询，使用批量查询和Map缓存提升性能。**
*   **连接池管理：** Prisma客户端使用单例模式，配置连接池，防止连接耗尽。
*   **API性能：** 对计算密集型或耗时长的API（如AI调用）进行异步处理，避免阻塞服务器。可以引入缓存机制（如Redis）来缓存常用数据，减少数据库查询。
*   **前端性能：** 优化前端代码，使用代码分割、懒加载等技术减少首屏加载时间。

### 7. 未来扩展性

*   **跨平台支持：** Web应用天然支持所有现代浏览器，无需额外开发即可在Windows, macOS, Linux, 移动设备上访问。
*   **移动端支持：** 可以使用Tauri或Capacitor将现有Web前端打包成原生App，或使用React Native复用部分组件和逻辑。
*   **架构扩展：** 后端可以从单体应用演进为微服务架构，以应对未来更复杂的功能和更高的并发。

### 8. 待定问题与风险

*   **开源词典数据的选择与整合：** 风险不变。需要在服务器端完成数据清洗和入库工作。
*   **艾宾浩斯记忆算法的具体参数设定：** 风险不变。需要在后端服务中实现该算法。
*   **AI API 的实际成本和额度：** 风险不变。由后端统一管理和调用。
*   **Web服务器和数据库的运维成本：** 新增风险。虽然初期在本地部署无成本，但未来上线需要考虑服务器和数据库的托管费用。
*   **复杂词书识别的挑战：** 风险不变。仍是长期研究方向。
