# 背单词软件设计想法总结

**版本:** v1.2
**日期:** 2025年11月13日
**作者:** AI 助手
**修订记录:**
| 版本 | 日期 | 描述 |
| :-- | :-- | :-- |
| 1.0 | 2025-10-25 | 初始设计想法总结 |
| 1.1 | 2025-10-26 | **重大更新：架构演进为Web应用，并深化核心学习与复习逻辑** |
| 1.2 | 2025-11-13 | **V2架构升级：简化为二层结构，partOfSpeech改为字符串，新增lemma支持** |

---

### 1. 核心设计哲学：从理想到实践

本项目的核心设计哲学在于 **优雅地处理复杂性**。单词学习的本质是复杂的，涉及到多义性、语境、记忆规律等多个维度。我们的设计目标不是回避这种复杂性，而是通过精巧的数据库结构和智能的业务逻辑，将其转化为一个清晰、高效、符合直觉的学习体验。

---

### 2. 架构的演进：从桌面到Web

*   **初始构想：** Electron + SQLite 桌面应用。
    *   **动因：** 聚焦单一平台，降低初期开发难度，快速验证MVP。
*   **最终决策：** **前后端分离的Web应用 (React + Node.js + MySQL)**。
    *   **动因：**
        1.  **更低的实现门槛：** 相比于需要处理打包、安装、系统兼容性的桌面应用，Web应用的开发和部署流程更为标准化和成熟，更利于快速迭代。
        2.  **天然的跨平台性：** 一次开发，所有拥有现代浏览器的设备（PC, Mac, Mobile）均可访问，完美解决了跨平台问题。
        3.  **数据集中管理：** 用户数据存储在云端，天然支持多端同步，为用户提供了无缝的学习体验。

---

### 3. 数据库设计的核心思想 (基石)

您最初的设计思想是整个项目的基石，我们在Web架构下对其进行了 **完整保留和增强**。

*   **思想一：分层存储，解耦属性 (V2简化)**
    *   **V1实现：** 通过 `words` -> `parts_of_speech` -> `meanings` 的三层结构，将一个单词的"拼写"、"词性"、"词义"三个不同维度的属性清晰地解耦。
    *   **V2优化：** 简化为 `words` -> `meanings` 二层结构，`partOfSpeech` 改为 `meanings` 表的字符串字段，减少JOIN查询，提升性能。
    *   **价值：** 在保持清晰度的同时，大幅简化查询逻辑，提高系统响应速度。同时新增 `lemma` 字段支持词形变化关系。

*   **思想二：例句复用，中央管理**
    *   **实现：** 设立 `example_pool` 作为所有例句的中央池，通过 `meaning_example_relation` 关联表，实现“一个例句可以服务于多个不同单词的词义”。
    *   **价值：** 极大地节省了存储空间，保证了例句的唯一性和一致性，也使得例句的管理（如更新、修正）变得异常简单。

*   **思想三：词书标签化，动态扩展**
    *   **实现：** 将词书视为一种“标签” (`book_tags`)，通过 `word_tag_relations` 关联表将单词“贴”上不同的词书标签。
    *   **价值：** 完全消除了传统设计中为每本词书复制一遍单词数据的巨大冗余，使得添加新词书、管理用户自定义词书变得极为高效和灵活。

---

### 4. 核心逻辑的深化 (演进与完善)

在从桌面端转向Web端，并深入思考多用户场景后，我们对核心的学习与复习逻辑进行了两次关键的深化，使整个系统更加严谨和智能。

*   **深化一：学习进度的跟踪粒度 — 从“单词”到“词义”**
    *   **问题：** 如果只跟踪用户对一个单词（如 `run`）的掌握度，就无法区分用户是对“奔跑”这个词义熟悉，还是对“经营”这个词义熟悉。
    *   **决策：** 将艾宾浩斯算法的核心——`user_learning_progress` 表的跟踪对象，从 `word_id` **精确到 `meaning_id`**。
    *   **价值：** 这是本次设计升级的 **灵魂**。它使得我们的软件能够真正实现对多义词的 **精准复习**。系统可以明确知道用户哪个词义不熟，并智能地推送相应的复习任务，完美契合了产品“深度学习多义词”的核心价值。

*   **深化二：复习方式的决策机制 — 引入“学习上下文”**
    *   **问题：** 当系统决定复习某个词义时，如何确定是用“拼写”还是“选义”模式？因为掌握要求（`mastery_focus`）是定义在“单词”与“词书”的关联上的。
    *   **决策：** 在 `users` 表中增加 `current_book_tag_id` 字段，引入“**用户当前学习词书**”这一上下文概念。
    *   **价值：** 彻底消除了复习方式选择的模糊性。复习逻辑变得清晰：**根据待复习词义，找到其所属单词，再结合用户当前学习的词书，查出唯一的掌握要求，最终确定复习方式。** 这使得复习模式的切换变得智能且符合逻辑。

---

### 5. 技术实现总结

*   **总体架构：** 前后端分离。
*   **前端 (Frontend):** React + TypeScript + Tailwind CSS。
*   **后端 (Backend):** Node.js + Express.js + TypeScript。
*   **数据库 (Database):** MySQL。
*   **ORM (推荐):** Prisma 或 TypeORM，用于保障数据库操作的类型安全和开发效率，并从根本上杜绝SQL注入。

---

### 6. 最终结论

通过本次全面的思考和重构，我们不仅完成了从桌面应用到Web应用的技术转型，更重要的是，我们深化并完善了产品的核心学习逻辑。现在的设计方案，既保留了您最初的精巧构思，又通过引入更精细的跟踪粒度和上下文机制，使其在多用户Web环境下变得更加强大、严谨和智能。这份设计思想总结，将是我们后续开发工作的最高指导原则。
